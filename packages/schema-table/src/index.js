import SchemaTable from './schema-table.vue'

// SchemaTable.install = () => {
//   SchemaTable.component(SchemaTable.name, SchemaTable);
// };

// if (typeof window !== undefined && window.Vue) {
//   window.Vue.use(SchemaTable);
// }

export default SchemaTable
